# Building the site only. Nothing here is a dependency of the library: `package.json`
# lists the pi SDK and nothing else, and `npm test` never reaches this file.
sphinx>=8.1
myst-parser[linkify]>=4.0
sphinx-design>=0.6
furo>=2024.8.6
sphinx-copybutton>=0.5.2
